name: corelasi_simple
description: CORELASI app (simple & split files)

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  flutter_local_notifications: ^17.2.2
  mobile_scanner: ^3.5.5
  # qr_code_scanner: ^1.0.1
  google_fonts: ^6.3.0
  intl: ^0.20.2
  camera: ^0.11.0+2

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
dev_dependencies:
  flutter_lints: 5.0.0
